global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Placeholder for the API service (uncomment once the API exposes /metrics):
  # - job_name: api
  #   static_configs:
  #     - targets: ["api:8000"]
