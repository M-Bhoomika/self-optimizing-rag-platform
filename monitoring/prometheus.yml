global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Uncomment when the API service is reachable from Prometheus.
  # - job_name: rag-api
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ["api:8000"]
